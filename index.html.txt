<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Доступ к видео</title>
    <style>
        body {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            font-family: 'Arial', sans-serif;
            background: #121212;
            color: #ffffff;
            text-align: center;
        }
        .hidden {
            display: none;
        }
        iframe {
            border: none;
            border-radius: 12px;
            margin-top: 20px;
            width: 90%;
            max-width: 400px;
            height: 225px;
        }
        button {
            background-color: #1c88ff;
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 8px;
            font-size: 16px;
            margin-top: 20px;
            cursor: pointer;
        }
        button:hover {
            background-color: #006be0;
        }
    </style>
</head>
<body>
    <h1 id="message">Проверяем вашу подписку...</h1>

    <div id="videoContainer" class="hidden">
        <iframe id="videoFrame" src="https://www.youtube.com/embed/dQw4w9WgXcQ" allowfullscreen></iframe>
    </div>

    <div id="subscribeContainer" class="hidden">
        <p>Чтобы получить доступ, подпишитесь на наш канал:</p>
        <button onclick="subscribe()">Подписаться</button>
    </div>

    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <script>
        const botToken = "7888624946:AAE5ZQ90Mj8U1-tAwT-CHmFzcO8TotJHgWM";  // <-- сюда вставить новый токен
        const channelUsername = "@hqvideosecret";

        function subscribe() {
            window.open(`https://t.me/${channelUsername.substring(1)}`, "_blank");
        }

        async function checkSubscription() {
            const userId = Telegram.WebApp.initDataUnsafe.user.id;

            const response = await fetch(`https://api.telegram.org/bot${botToken}/getChatMember?chat_id=${channelUsername}&user_id=${userId}`);
            const data = await response.json();

            if (data.result && (data.result.status === "member" || data.result.status === "administrator" || data.result.status === "creator")) {
                document.getElementById('message').innerText = "Доступ открыт ✅";
                document.getElementById('videoContainer').classList.remove('hidden');
            } else {
                document.getElementById('message').innerText = "Доступ закрыт ❌";
                document.getElementById('subscribeContainer').classList.remove('hidden');
            }
        }

        Telegram.WebApp.ready();
        checkSubscription();
    </script>
</body>
</html>